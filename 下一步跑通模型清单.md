# 下一步：直接用平台数据和公共模型路径跑通

## 当前目标

先不要再用 BraTS。现在按老师要求，直接围绕云平台数据和比赛提供的模型路径跑一个最小闭环：

平台训练标注数据 -> 生成 nnU-Net 数据格式 -> 训练 Core/Total 两个分割模型 -> 启动推理服务 -> 输出 `prediction.json` 和 mask。

## 已知平台路径

```text
训练标注数据：/2026aicompetition/datasets/training/annotation
训练标签表：  /2026aicompetition/datasets/training/label
公共 nnU-Net：/2026aicompetition/public_models/MIC-DKFZ/nnUNet
输出结果：    /2026aicompetition/workspace/answer
日志目录：    /2026aicompetition/workspace/logs
```

## 先做这三步

1. 检查平台数据能否被代码识别：

```bash
python glioma_seg_baseline/scripts/validate_competition_dataset.py \
  --dataset-path /2026aicompetition/datasets/training/annotation \
  --output-csv /2026aicompetition/workspace/logs/td_segment_dataset_check.csv
```

2. 生成 nnU-Net 训练数据：

```bash
MAX_CASES=8 bash glioma_seg_baseline/scripts/prepare_platform_nnunet_data.sh
```

确认能成功后再去掉 `MAX_CASES=8` 跑更多病例。

3. 启动训练：

```bash
bash glioma_seg_baseline/scripts/train_nnunet_baseline.sh
```

## 判断是否跑通

- 能生成 `Dataset401_GliomaCoreT1CE`。
- 能生成 `Dataset402_GliomaTotalFLAIR`。
- `nnUNetv2_plan_and_preprocess -d 401/402` 能通过。
- 训练能产生 checkpoint。
- 推理服务能在 `/2026aicompetition/workspace/answer/{evaluation_id}` 下写出 mask 和 `prediction.json`。

## 注意

当前版本是单序列双模型基线，不是最终多模态融合模型。先用它验证平台数据、训练命令和提交接口，再考虑多模态模型或集成。
