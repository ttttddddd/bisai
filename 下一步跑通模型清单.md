# 下一步：先用 BraTS 小样本跑通模型

## 目标

先不要追求比赛平台数据完全一致，也不要先调复杂模型。当前最重要的是跑通一个最小闭环：

BraTS 小样本 -> 生成 nnU-Net 数据格式 -> 训练 -> 验证/预测 -> 得到 Core mask 和 Total Abnormal mask。

## 为什么可以用 BraTS

BraTS 和赛道四在原理上高度接近：

- 都是脑胶质瘤 MRI。
- 都是 3D NIfTI 影像。
- 都有 T1、T1CE/T1c、T2、FLAIR/T2-FLAIR。
- BraTS 标签可以派生比赛需要的两个代理目标：
  - Core 近似使用 BraTS TC：label 1 + label 4。
  - Total Abnormal 近似使用 BraTS WT：seg > 0。

但注意：BraTS 是代理数据，不等于比赛平台真实数据。第一阶段只用于把模型和流程跑通。

## 先选哪个模型

建议第一版用 `nnU-Net v2`。

`model_all.docx` 里和当前任务最相关的公共模型路径：

- `/2026aicompetition/public_models/MIC-DKFZ/nnUNet`
- `/2026aicompetition/public_models/MIC-DKFZ/MedNeXt`
- `/2026aicompetition/public_models/Project-MONAI`

第一阶段建议固定 nnU-Net，不要一开始换 MedNeXt/Swin UNETR。先确认数据、标签、训练、预测能通。

## 你接下来按这个顺序做

1. 准备 5-10 个 BraTS 病例。
   - 每个病例至少要有 `t1n/t1c/t2w/t2f/seg` 五个文件。
   - 先不要全量数据，越小越容易排错。

2. 在云电脑或本地环境准备 Python 依赖。
   - 转换 NIfTI 需要 `nibabel` 和 `numpy`。
   - 当前本地环境缺 `nibabel`，所以脚本需要在装好依赖后运行。

3. 用本目录脚本生成 nnU-Net 小样本数据。

   ```bash
   python tools/brats_to_nnunet_mini.py \
     --brats-root /path/to/BraTS \
     --out-root /path/to/nnUNet_raw \
     --num-cases 8
   ```

   输出会生成两个数据集：

   - `Dataset401_GliomaCore`
   - `Dataset402_GliomaTotal`

4. 跑 nnU-Net 的 dataset fingerprint / preprocess。
   - 先跑 `Dataset401_GliomaCore`。
   - 成功后再跑 `Dataset402_GliomaTotal`。

5. 训练一个很小的配置。
   - 目标不是高分，是确认能开始训练、loss 能下降、能保存 checkpoint。
   - 如果显存紧张，先用 3d_lowres 或减少训练轮数。

6. 做一次预测。
   - 输入验证病例。
   - 输出 `.nii.gz` mask。
   - 先人工看 mask 是否不是全空、不是全图都是 1。

7. 再补比赛接口。
   - 训练跑通后，再写 `/call` 服务。
   - 推理服务需要监听 8000 端口。
   - 结果要写到 `/2026aicompetition/workspace/answer/{evaluation_id}`。

## 判断是否跑通

最低标准：

- 能读入 4 个 MRI 模态。
- 能读入 BraTS seg 并转成 Core/Total 二值 mask。
- nnU-Net 能完成 preprocess。
- 训练至少能跑几个 epoch。
- 能输出预测 mask。
- 输出 mask 的文件名和路径后续能改成比赛要求。

## 暂时不要做的事

- 不要先做完整造数据方案。
- 不要先追求平台目录完全仿真。
- 不要先上 MedNeXt、Swin UNETR 或复杂 ensemble。
- 不要一开始全量 BraTS 训练。

先跑通，再逐步逼近比赛平台数据接口。
